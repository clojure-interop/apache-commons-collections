(ns org.apache.commons.collections4.map.AbstractLinkedMap$LinkEntry
  "LinkEntry that stores the data.

  If you subclass AbstractLinkedMap but not LinkEntry
  then you will not be able to access the protected fields.
  The entryXxx() methods on AbstractLinkedMap exist
  to provide the necessary access."
  (:refer-clojure :only [require comment defn ->])
  (:import [org.apache.commons.collections4.map AbstractLinkedMap$LinkEntry]))

